# Update → 3.52.0

Ten files. Upload all ten: `https://github.com/lballaty/OpenGraph/upload/main`

## Select all

It was behind Cmd+A, which does not exist on an iPad, so the button was the only route
and it sat inside a scrolling list. Pinned now, beside Undo and Settings.

## And a long press always offers the way out

Three reports today were the same shape — Select unreachable, a floating bar's top
button unreachable, Select all unreachable. Pinning helps; a gesture is stronger,
because it cannot scroll away or be hidden behind a panel.

Long-press empty canvas and the menu now ends with **Always here**: Select, Select all,
Undo. Whatever state the toolbars are in.

## Also in this bundle

- **3.51.0** zoom out to about 13,660 km, and it tells you when you reach the limit
- **3.50.0** the scale bar says how wide the view is
- **3.49.2** the details panel selects its own text, so Copy is one press

## After uploading

Bottom-right should read `3.52.0`. Try a long press on empty canvas — the last three
entries should be Select, Select all and Undo.
